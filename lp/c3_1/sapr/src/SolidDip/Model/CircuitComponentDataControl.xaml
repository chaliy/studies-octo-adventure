<UserControl x:Class="SolidDip.Model.CircuitComponentDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolidDip.Model"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=xctk:PropertyItem}"
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        
        <TextBox Grid.Column="0" Text="{Binding Value}" />
        <Button Grid.Column="1" Click="Button_Click">...</Button>
    </Grid>
</UserControl>
